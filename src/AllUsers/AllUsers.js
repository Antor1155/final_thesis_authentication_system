import React from 'react';
import auth from '../utilities/firebase.init';

const AllUsers = () => {
    console.log(auth);
    return (
        <div>
            
        </div>
    );
};

export default AllUsers;